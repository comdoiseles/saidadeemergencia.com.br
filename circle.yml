machine:
  environment:
    USER_NAME: lnmunhoz
    USER_EMAIL: ln.munhoz@gmail.com
  ruby:
    version: 2.2.3

dependencies:
  pre:
    - ./bin/setup

deployment:
  production:
    branch: master
    commands:
      - ./bin/automated
